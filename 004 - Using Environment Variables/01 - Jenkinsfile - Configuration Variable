pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
	envrioment{
		NOTIFICATION_EMAIL='ola@ola.com'
	}
    stages {
        stage('build') {
            steps {
                echo "$NOTIFICATION_EMAIL"
            }
        }
    }
}
