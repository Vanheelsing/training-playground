pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
	enviroment{
		NOTIFICATION_EMAIL='ola@ola.com'
	}
    stages {
        stage('build') {
            steps {
                echo "$NOTIFICATION_EMAIL"
            }
        }
    }
}
