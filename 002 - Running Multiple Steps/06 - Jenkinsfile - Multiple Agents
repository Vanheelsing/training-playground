pipeline {
    agent none
    stages {
    	stage("Maven"){
		agent {docker { image 'maven:latest' }}
		steps{
			sh 'maven --version'
		}
	}
	stage("Npm"){
		agent {docker { image 'npm:latest' }}
		steps{
			sh 'npm --version'
		}
	}
    }
}
