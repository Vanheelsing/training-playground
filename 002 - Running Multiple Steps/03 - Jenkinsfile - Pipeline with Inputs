pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    parameters {
        	string(name: 'NAME', defaultValue: 'Eu', description: 'Who should I say hello to?')
		string(age: 'AGE', defaultValue: '22', description: 'Who should I say hello to?')
	}
    stages {
        stage('build') {
            steps {
                echo "$NAME"
                echo "$AGE"
            }
        }
    }
}
