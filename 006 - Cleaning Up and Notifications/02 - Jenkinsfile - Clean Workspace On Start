pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
	    	sh "ls -lta"
	    	cleanWs()
                sh "date > data.txt"
		sh "ls -lta"
            }
        }
    }
}
