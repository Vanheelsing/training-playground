pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
	    	cleanWs()
	    	sh "ls -lta"
                sh "date > data.txt"
		sh "ls -lta"
            }
        }
    }
}
